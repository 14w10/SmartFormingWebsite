export enum MODULE_TYPES {
  'pre-fe' = 'Pre-FE',
  'post-fe' = 'Post-FE',
}

export enum MODULE_ACTIONS {
  approve = 'approve',
  underReview = 'under review',
  publish = 'publish',
  reject = 'reject',
}
